<%- include('partials/header') %>
<div class="container" style="max-width:400px;">
  <h2 class="mb-4 text-center">Digite o Token</h2>
  <% if (error) { %>
    <div class="alert alert-danger"><%= error %></div>
  <% } %>
  <form action="/login/token" method="POST">
    <div class="form-group">
        <label for="token">Token recebido por SMS:</label>
        <input type="text" class="form-control" id="token" name="token" required maxlength="6" minlength="6" placeholder="6 dígitos">
    </div>
    <button type="submit" class="btn btn-success w-100">Validar Token</button>
  </form>
</div>
<%- include('partials/footer') %>